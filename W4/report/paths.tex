%
% path.tex
%

\section{Paths}
Our solution of the \code{ispath} problem walks through the path list
recursively, on each pass checking whether or not there is a directional
relationship (see appendix \ref{appendix:drel}), in accordance with $D$
between $X$ and $Y$.

\codefig{ispath}{\assignment}{76}{79}{The implementation of \code{ispath}}
